name: Publish

on:
  release:
    types: [published]

jobs:
  publish-to-npm:
    if: github.repository_owner == '1ucky40nc3'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
      - run: node scripts/publish-to-npm.js
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_AUTH_TOKEN }}
